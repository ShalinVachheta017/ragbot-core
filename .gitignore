# === Private data & runtime artifacts (all under data/) ===
data/
extractdirect/          # legacy extraction dir (old runs)
_corrupt/               # quarantine for bad files
.qodo/                  # local tool metadata

# === Logs (root or legacy) ===
logs/
*.log

# === Derived metadata / DBs (legacy names; new ones live under data/) ===
metadata/ingest_log_*.csv
metadata/cleaned_metadata.csv
metadata/cleaned_metadata.xlsx
metadata/manifest.db
*.sqlite
*.db
processed_hashes.json

# If you ever mount Qdrant or other storage locally, ignore it
qdrant_storage/

# === Python build & cache ===
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/

# === Environments ===
.venv/
venv/
env/

# === Secrets / local weights ===
.env
.env.*
*.bin
*.pt
*.ckpt
*.safetensors
*.onnx
*.gguf

# === OS cruft ===
.DS_Store
Thumbs.db

# === Editors / IDE ===
.vscode/
.idea/

# === Temp / cache ===
*.tmp
*.bak
*.swp
.ipynb_checkpoints/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# === Project-specific (keep out of git) ===
/Test\ Data\ of\ Tender\ Files/
/notes.txt
how_what_why.ipynb
