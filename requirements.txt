#---- Vector DB ----
qdrant-client>=1.9,<2.0

# SentenceTransformer stack (GPU/CPU works; uses your installed torch)
sentence-transformers>=3.0.1
transformers>=4.40,<5
# Pin torch to a safe band; install the CUDA build if you have NVIDIA GPU
#torch>=2.1 download as per your requirements I have used GPU one


# === Data / PDF / utils ===
numpy>=1.24,<3
pandas>=2.2
openpyxl>=3.1
pymupdf>=1.24
tqdm>=4.66
langdetect>=1.0.9

# Windows magic for filetype sniffing (safe no-op on non-Windows)
python-magic-bin==0.4.14 ; sys_platform == "win32"
     

# ---- LLM / UI ----
ollama>=0.3.0
streamlit>=1.36.0

# Optional OCR for scanned PDFs (enable later if needed)
# pytesseract