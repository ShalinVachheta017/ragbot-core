# === Vector DB + Embeddings ===
qdrant-client>=1.8.4,<1.10

# SentenceTransformer stack (GPU/CPU works; uses your installed torch)
sentence-transformers>=3.0.1
transformers>=4.40,<5
# Pin torch to a safe band; install the CUDA build if you have NVIDIA GPU


# === Data / PDF / utils ===
numpy>=1.24,<3
pandas>=2.2
openpyxl>=3.1
pymupdf>=1.24
tqdm>=4.66
langdetect>=1.0.9

# Windows magic for filetype sniffing (safe no-op on non-Windows)
python-magic-bin==0.4.14 ; sys_platform == "win32"

# Optional: if you run an API later, uncomment these
# fastapi>=0.111
# uvicorn[standard]>=0.30
